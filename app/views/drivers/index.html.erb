<h2>Drivers</h2>

<%= form_tag by_name_path, method: :get do %>
  <%= label_tag :name %>
  <%= text_field_tag :name %>
  <%= submit_tag %>
<% end %>

<% @drivers.each do |driver| %>
  <p> <%= link_to "#{driver.name}", driver_path(driver.id) %> </p>
<%end%>
